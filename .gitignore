# MoonDeck deck — track the source, ignore build output + app-regenerated wiring.
# Built deck (rebuilt from slides.html on every open):
*.html
!slides.html
# Slide screenshots + audit renders (scripts/shot.js, scripts/audit.js):
shots/
audit/
# Skill wiring + instructions the app reseeds on every open:
.claude/
.agents/
AGENTS.md
CLAUDE.md
.DS_Store
